import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sycamoresstake.ym-camp',
  appName: 'Sycamores Stake Young Men Camp',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
